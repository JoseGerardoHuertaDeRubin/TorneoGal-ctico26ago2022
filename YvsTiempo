function [fitresult, gof] = createFit(Tiempo, PosicionY)
%CREATEFIT(TIEMPO,POSICIONY)
%  Create a fit.
%
%  Data for 'Y vs Tiempo' fit:
%      X Input: Tiempo
%      Y Output: PosicionY
%  Output:
%      fitresult : a fit object representing the fit.
%      gof : structure with goodness-of fit info.
%
%  See also FIT, CFIT, SFIT.

%  Auto-generated by MATLAB on 28-Aug-2022 15:12:22


%% Fit: 'Y vs Tiempo'.
[xData, yData] = prepareCurveData( Tiempo, PosicionY );

% Set up fittype and options.
ft = fittype( 'poly1' );

% Fit model to data.
[fitresult, gof] = fit( xData, yData, ft );

% Plot fit with data.
figure( 'Name', 'Y vs Tiempo' );
h = plot( fitresult, xData, yData );
legend( h, 'PosicionY vs. Tiempo', 'Y vs Tiempo', 'Location', 'NorthEast', 'Interpreter', 'none' );
% Label axes
xlabel( 'Tiempo', 'Interpreter', 'none' );
ylabel( 'PosicionY', 'Interpreter', 'none' );
grid on


